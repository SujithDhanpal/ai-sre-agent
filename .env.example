# === REQUIRED ===
ANTHROPIC_API_KEY=

# === OPTIONAL — Embeddings ===
OPENAI_API_KEY=

# === OPTIONAL — Grafana (logs + metrics) ===
GRAFANA_ENABLED=false
GRAFANA_LOKI_URL=
GRAFANA_PROMETHEUS_URL=
GRAFANA_API_KEY=

# === OPTIONAL — GitHub (code + PRs) ===
GITHUB_ENABLED=false
GITHUB_TOKEN=
GITHUB_OWNER=

# === OPTIONAL — Sentry (error tracking) ===
SENTRY_ENABLED=false
SENTRY_AUTH_TOKEN=
SENTRY_ORG=

# === OPTIONAL — AWS (infrastructure) ===
AWS_ENABLED=false
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# === OPTIONAL — PostgreSQL diagnostics (direct DB access) ===
POSTGRES_DIAG_ENABLED=false
POSTGRES_DIAG_URL=
POSTGRES_DIAG_USER=
POSTGRES_DIAG_PASSWORD=

# === OPTIONAL — SSH / Kube (for skills that need bastion access) ===
SSH_KEY_PATH=
KUBE_CONFIG_PATH=

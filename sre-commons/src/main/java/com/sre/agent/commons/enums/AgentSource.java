package com.sre.agent.commons.enums;

public enum AgentSource {
    PLATFORM,       // Built-in agents shipped with the product
    TENANT,         // Custom agents created by a specific tenant
    MARKETPLACE     // Shared agents published to the marketplace
}
